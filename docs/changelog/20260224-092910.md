# 20260224-092910 新增用户收藏作品测试脚本

## 任务
新增测试方法，用于获取用户所有收藏作品。

## 待办
- [x] 分析现有代码结构（handler、crawler、filter）
- [x] 创建收藏作品测试脚本

## 执行结果
新增 `examples/test-user-collection.ts`，基于以下已有能力：
- `DouyinHandler.fetchUserCollectionVideos()` — 异步生成器，支持分页
- `UserCollectionFilter` — 继承自 `UserPostFilter`，`maxCursor` 读取 `$.cursor`
- API endpoint: `USER_COLLECTION`（`/aweme/v1/web/aweme/listcollection/`）

## 使用方式
```bash
# 获取全部收藏
DOUYIN_COOKIE="..." npx tsx examples/test-user-collection.ts

# 获取前20个
DOUYIN_COOKIE="..." npx tsx examples/test-user-collection.ts 20
```
